---
title: Development Setup
description: Setting up your development environment for contributing to CyanPrint
---

# Development Setup

This guide will help you set up your development environment for contributing to CyanPrint.

## Prerequisites

CyanPrint uses **Nix** for reproducible development environments. All dependencies are managed through Nix flakes.

| Tool | Purpose |
| ---- | ------- |
| Nix | Package manager and dev environment |
| Direnv | Auto-loads Nix environment |
| Git | Version control |

## Quick Start

### 1. Fork and Clone

```bash
# Fork the repository on GitHub, then clone your fork
git clone https://github.com/YOUR_USERNAME/sulfone.REPO_NAME.git
cd sulfone.REPO_NAME
```

### 2. Enter Development Environment

```bash
# Allow direnv to load the environment
direnv allow

# Nix will automatically install all required dependencies
```

### 3. Setup and Run

```bash
# Setup dependencies
pls setup

# Start development
pls dev
```

## Common Commands

All repositories use `pls` (Taskfile) for common operations:

| Command | Description |
| ------- | ----------- |
| `pls setup` | Install dependencies |
| `pls dev` | Start development server |
| `pls build` | Build for production |
| `pls test` | Run tests (if available) |

## Pre-commit Hooks

Pre-commit hooks run automatically on commit and handle:
- Code formatting (Biome, treefmt)
- Linting
- Secrets scanning
- Shellcheck

To run all checks manually:

```bash
pre-commit run -a
```

## Next Steps

- Read the [Architecture Overview](/docs/contributor/architecture/overview)
- Explore the [Repositories](/docs/contributor/repositories)
