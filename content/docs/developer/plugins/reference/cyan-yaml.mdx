---
title: Plugin cyan.yaml
description: Configuration file for plugin metadata
---

import { Callout } from 'fumadocs-ui/components/callout';

# Plugin cyan.yaml

The `cyan.yaml` file provides metadata about your plugin for the Cyan registry. This file is required for publishing your plugin.

<Callout type="info">
This page describes the **plugin** cyan.yaml schema. Template cyan.yaml files have different fields like `processors`, `plugins`, and `templates` arrays. See the [Template cyan.yaml](/developer/templates/reference/cyan-yaml) documentation for that schema.
</Callout>

## Basic Structure

```yaml
username: atomi
name: my-plugin
description: A plugin that performs post-processing operations
project: https://github.com/atomi/my-plugin
source: https://github.com/atomi/my-plugin.git
email: admin@atomi.cloud
tags: [formatting, post-process]
readme: README.MD
```

## Properties

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `username` | `string` | Yes | Owner username (who publishes the plugin) |
| `name` | `string` | Yes | Plugin name (alphanumeric, dashes allowed) |
| `description` | `string` | Yes | Brief description of what the plugin does |
| `project` | `string` | Yes | Project homepage URL |
| `source` | `string` | Yes | Source repository URL |
| `email` | `string` | Yes | Contact email for the plugin maintainer |
| `tags` | `string[]` | Yes | Search tags for discovery |
| `readme` | `string` | Yes | Path to README file (usually `README.MD`) |

## Full Example

```yaml
username: atomi
name: setup-plugin
description: Plugin that sets up git, installs dependencies, and configures the project
project: https://github.com/atomi/cyan-plugin-setup
source: https://github.com/atomi/cyan-plugin-setup.git
email: admin@atomi.cloud
tags:
  - setup
  - git
  - npm
  - initialization
readme: README.MD
```

## Plugin vs Template cyan.yaml

Plugins and templates use different cyan.yaml schemas:

- **Plugin cyan.yaml**: Contains only metadata (`username`, `name`, `description`, `project`, `source`, `email`, `tags`, `readme`)
- **Template cyan.yaml**: Contains additional fields like `processors`, `plugins`, and `templates` arrays to define the template's dependencies

<Callout type="info">
Plugin cyan.yaml does not define a `config` schema. Configuration is passed at runtime by the template when invoking the plugin. See [Conditional Execution](/developer/plugins/how-to/conditional-execution) for how templates pass config to plugins.
</Callout>

## Usage in Templates

Templates reference plugins using the `username/name` format with an optional version:

```yaml
# In template's cyan.yaml
plugins:
  - atomi/setup-plugin:1
  - atomi/formatter
```

## Versioning

Versions are managed by the Cyan registry, not declared in the plugin's cyan.yaml. When referencing a plugin in a template:

```yaml
# Reference without version (uses latest)
plugins:
  - atomi/setup-plugin

# Reference with specific version (integer)
plugins:
  - atomi/setup-plugin:3
```

## Related

- [Project Structure](/developer/plugins/reference/project-structure) - Directory layout
- [Push to Registry](/developer/plugins/how-to/push-to-registry) - Publishing your plugin
- [Conditional Execution](/developer/plugins/how-to/conditional-execution) - Using config at runtime
