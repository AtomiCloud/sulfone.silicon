---
title: Processor Input/Output
description: Type definitions for processor interfaces
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Cards, Card } from 'fumadocs-ui/components/card';

# Processor Input/Output

Type definitions for the data structures passed to and returned from processors.

## CyanProcessorInput

The input object passed to the processor handler.

### Definition

```ts -c
interface CyanProcessorInput {
  readDir: string;
  writeDir: string;
  globs: CyanGlob[];
  config: unknown;
}
```

### Properties

<TypeTable
  type={{
    readDir: {
      type: 'string',
      description: 'Path to source files',
    },
    writeDir: {
      type: 'string',
      description: 'Path for output files',
    },
    globs: {
      type: 'CyanGlob[]',
      description: 'File patterns from template',
    },
    config: {
      type: 'unknown',
      description: 'Configuration from template',
    },
  }}
/>

### Typical Values

| Property | Typical Value |
|----------|--------------|
| `readDir` | `/workspace/cyanprint/` |
| `writeDir` | `/workspace/output/` |

### Usage

```ts -c
StartProcessorWithLambda(async (input, fileHelper) => {
  console.log('Reading from:', input.readDir);
  console.log('Writing to:', input.writeDir);
  console.log('Globs:', input.globs);
  console.log('Config:', input.config);

  // Always return writeDir
  return { directory: input.writeDir };
});
```

---

## ProcessorOutput

The return value from the processor handler.

### Definition

```ts -c
interface ProcessorOutput {
  directory: string;
}
```

### Properties

<TypeTable
  type={{
    directory: {
      type: 'string',
      description: 'Output directory path',
    },
  }}
/>

### Usage

```ts -c
StartProcessorWithLambda(async (input, fileHelper) => {
  // ... processing ...

  // Must return the write directory
  return { directory: input.writeDir };
});
```

---

## CyanGlob

Defines which files to process.

### Definition

```ts -c
interface CyanGlob {
  root?: string | null;
  glob: string;
  exclude: string[];
  type: GlobType;
}
```

### Properties

<TypeTable
  type={{
    root: {
      type: 'string | null',
      required: false,
      description: 'Base directory for glob',
    },
    glob: {
      type: 'string',
      required: true,
      description: 'Glob pattern',
    },
    exclude: {
      type: 'string[]',
      required: true,
      description: 'Patterns to exclude (can be empty array)',
    },
    type: {
      type: 'GlobType',
      required: true,
      description: 'Type of glob operation',
    },
  }}
/>

### Examples

```ts -c
// All files in templates directory
{ root: 'templates', glob: '**/*', exclude: [], type: GlobType.Template }

// Only markdown files
{ root: 'docs', glob: '**/*.md', exclude: [], type: GlobType.Template }

// TypeScript files excluding tests
{
  root: 'src',
  glob: '**/*.ts',
  exclude: ['**/*.test.ts', '**/*.spec.ts'],
  type: GlobType.Template
}

// Multiple patterns (as array element)
[
  { root: 'src', glob: '**/*.ts', exclude: [], type: GlobType.Template },
  { root: 'styles', glob: '**/*.css', exclude: [], type: GlobType.Template }
]
```

---

## GlobType

Enum defining the type of glob operation.

### Definition

```ts -c
enum GlobType {
  Template = 0,
  Copy = 1
}
```

### Values

<TypeTable
  type={{
    Template: {
      type: '0',
      description: 'Files are processed as templates',
    },
    Copy: {
      type: '1',
      description: 'Files are copied without processing',
    },
  }}
/>

### Glob Syntax

<TypeTable
  type={{
    '*': {
      type: 'pattern',
      description: 'Any file name',
    },
    '**': {
      type: 'pattern',
      description: 'Any directory depth',
    },
    '**/*': {
      type: 'pattern',
      description: 'All files recursively',
    },
    '*.md': {
      type: 'pattern',
      description: 'Files with .md extension',
    },
    'src/**/*.ts': {
      type: 'pattern',
      description: '.ts files under src',
    },
    '!': {
      type: 'prefix',
      description: 'Negation (in exclude array)',
    },
  }}
/>

---

## Config Type

The `config` property is typed as `unknown`. Cast to your expected type:

### Definitions

```ts -c
// In your processor
interface MyProcessorConfig {
  // Define expected config shape
  transformType: 'uppercase' | 'lowercase';
  includeHeader: boolean;
  customPrefix?: string;
}

StartProcessorWithLambda(async (input, fileHelper) => {
  // Cast to your type
  const config = input.config as MyProcessorConfig;

  // Or provide defaults
  const config: MyProcessorConfig = {
    transformType: 'uppercase',
    includeHeader: false,
    ...(input.config as Partial<MyProcessorConfig>)
  };
});
```

### Complex Config

```ts -c
interface ProcessorConfig {
  // Simple types
  enabled: boolean;
  maxSize: number;
  outputPath: string;

  // Arrays
  includePatterns: string[];

  // Nested objects
  formatting: {
    indent: number;
    newline: 'lf' | 'crlf';
  };

  // Optional properties
  customHandler?: string;
}
```

---

## Full Example

```ts -c
import { StartProcessorWithLambda } from '@atomicloud/cyan-sdk';

// Define your config type
interface MarkdownConfig {
  generateToc: boolean;
  headingAnchors: boolean;
  maxDepth?: number;
}

StartProcessorWithLambda(async (input, fileHelper) => {
  // Parse config with defaults
  const config: MarkdownConfig = {
    generateToc: false,
    headingAnchors: true,
    ...(input.config as Partial<MarkdownConfig>)
  };

  // Log input information
  console.log('Processor Input:');
  console.log('  Read directory:', input.readDir);
  console.log('  Write directory:', input.writeDir);
  console.log('  Globs:', JSON.stringify(input.globs, null, 2));
  console.log('  Config:', JSON.stringify(config, null, 2));

  // Load files based on globs
  const files = fileHelper.resolveAll();

  // Process files
  files.forEach(file => {
    if (config.generateToc) {
      file.content = addTableOfContents(file.content, config.maxDepth);
    }

    if (config.headingAnchors) {
      file.content = addHeadingAnchors(file.content);
    }

    file.writeFile();
  });

  // Return output
  return { directory: input.writeDir };
});
```

## Related

<Cards>
  <Card title="StartProcessorWithLambda" href="/docs/developer/processors/reference/sdk/start-processor" description="Entry point" />
  <Card title="CyanFileHelper" href="/docs/developer/processors/reference/sdk/file-helper" description="File operations" />
  <Card title="Types" href="/docs/developer/processors/reference/sdk/types" description="All type definitions" />
</Cards>
